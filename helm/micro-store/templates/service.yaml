{{- range $name, $service := .Values.services }}
{{- if $service.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app: {{ $name }}
spec:
  type: {{ default $.Values.common.serviceType $service.serviceType }}
  ports:
  - port: {{ $service.servicePort }}
    targetPort: {{ default $service.servicePort $service.containerPort }}
  selector:
    app: {{ $name }}
{{- end }}
{{- end }}
